---
name: Docker Image CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Linter
      run: docker run -v.:/go make lint
    - name: docker build
      run: docker build -t plexpod .
